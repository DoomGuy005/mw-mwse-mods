local default = {
    key = {
        keyCode = tes3.scanCode.q,
        isShiftDown = false,
        isAltDown = false,
        isControlDown = false
    }
}

return mwse.loadConfig("placeSwap", default)